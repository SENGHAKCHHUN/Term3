<?php
    $numbers = [1, 2, 3, 4];
    // Add new element at the last 
    array_push($numbers, 10);
    // Delete last element
    array_pop($numbers);
    // Delete first element 
    array_shift($numbers);
    // Add new element at the first 
    array_unshift($numbers, 50);
?>