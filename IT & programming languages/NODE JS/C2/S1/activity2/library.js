const minNumber = (a, b) => {
    return Math.min(a, b);
}

const maxNumber = (a, b) => {
    return Math.max(a, b);
}

const PI = 3.14;
const COVID = 19;

module.exports.minNumber = minNumber;
module.exports.COVID = COVID;